systemctl stop s-ui 2>/dev/null || true; systemctl disable s-ui 2>/dev/null || true; rm -f /etc/systemd/system/s-ui.service; rm -rf /usr/local/s-ui; rm -f /usr/local/bin/s-ui /usr/bin/s-ui /usr/local/bin/sui /usr/bin/sui; systemctl daemon-reload; systemctl reset-failed; echo -e "\n====================\ns-ui 面板已完全卸载！\n===================="
